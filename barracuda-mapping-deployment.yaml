apiVersion: apps/v1
kind: Deployment
metadata:
  name: barracuda-mapping
  labels:
    app: barracuda-mapping
spec:
  replicas: 1
  selector:
    matchLabels:
      app: barracuda-mapping
  template:
    metadata:
      labels:
        app: barracuda-mapping
    spec:
      nodeSelector:
        kubefamily: jetson
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: barracuda-mapping
          image: ghcr.io/usc-robosub/barracuda-mapping:main
          command: ["/bin/bash", "/opt/barracuda-mapping/entrypoint.sh"]
          env:
            - name: ROS_MASTER_URI
              value: "http://barracuda-description.default.svc.cluster.local:11311"
            - name: ROS_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          tty: true
          securityContext:
            runAsUser: 0
      restartPolicy: Always
