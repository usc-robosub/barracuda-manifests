apiVersion: apps/v1
kind: Deployment
metadata:
  name: barracuda-navigation
  labels:
    app: barracuda-navigation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: barracuda-navigation
  template:
    metadata:
      labels:
        app: barracuda-navigation
    spec:
      nodeSelector:
        kubefamily: jetson
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: barracuda-navigation
          image: ghcr.io/usc-robosub/barracuda-navigation:main
          command: ["/bin/bash", "/opt/barracuda-navigation/entrypoint.sh"]
          env:
            - name: ROS_MASTER_URI
              value: "http://barracuda-description.default.svc.cluster.local:11311"
            - name: ROS_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          tty: true
          securityContext:
            runAsUser: 0
      restartPolicy: Always
