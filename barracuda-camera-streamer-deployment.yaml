apiVersion: apps/v1
kind: Deployment
metadata:
  name: barracuda-camera-streamer
  labels:
    app: barracuda-camera-streamer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: barracuda-camera-streamer
  template:
    metadata:
      labels:
        app: barracuda-camera-streamer
    spec:
      nodeSelector:
        kubefamily: jetson
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: barracuda-camera-streamer
          image: ghcr.io/usc-robosub/barracuda-camera-streamer:latest
          command: ["/bin/bash", "/opt/barracuda-camera-streamer/entrypoint.sh"]
          env:
            - name: ROS_MASTER_URI
              value: "http://barracuda-description.default.svc.cluster.local:11311"
            - name: ROS_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          tty: true
          securityContext:
            runAsUser: 0
      restartPolicy: Always
